# @hkyhy/marsun-components-core v0.1.72

- 新增 `@hkyhy/marsun-components-core/iam`：IamUsers/Roles/Org/SharedGroups 薄页 + `IamClient` / draft EP utils
- PermissionBindPanel：右侧内容区留白加大
- AgentAppShell：`--content-padding` 回退默认改为 `16px`（与 QA/设备 tokens 对齐；padding 仍由业务主区滚动容器承担）
