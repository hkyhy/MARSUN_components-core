# @hkyhy/marsun-components-core v0.1.73

- `ModulePageShell`：新增 `fillHeight`（默认 `true`）；`false` 时高度随内容撑开，交给外层 VirtualScrollbar
- `PageSpin`：新增 `naturalHeight`，配合文档流列表页
- IAM 四页（Users/Roles/Org/SharedGroups）：`fillHeight={false}`，修复 S3/设备权限页无法滚动
