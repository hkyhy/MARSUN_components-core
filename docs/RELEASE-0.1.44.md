# v0.1.44

- ReactFilter（kne 移植）：新页列表筛选 SSOT；`FilterValueDisplay` hover 小号字 + max-height
- SuperSelect / SelectTree 弹层全局修补（`superSelectPopup.scss`）
- CI：`tsconfig` `allowJs` 以支持 ReactFilter `.jsx` typecheck
